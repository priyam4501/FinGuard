# Copy to .env or export before running `./mvnw spring-boot:run`
DB_URL=jdbc:postgresql://localhost:5432/finguard
DB_USER=postgres
DB_PASSWORD=ps4501

# 64+ char random string. Generate with: openssl rand -base64 64
JWT_SECRET=change-me-to-a-long-random-string-at-least-64-chars-long-please-do-it
JWT_EXPIRY_MINUTES=1440

APP_CORS_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:3000
SERVER_PORT=8083
